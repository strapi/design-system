<!--- Dialog.stories.mdx --->

import { useState } from 'react';
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { ArgsTable } from '@storybook/addon-docs';
import { Text } from '../Text';
import { Stack } from '../Stack';
import { Row } from '../Row';
import { Box } from '../Box';
import { Button } from '../Button';
import { Dialog } from './Dialog';
import { DialogBody } from './DialogBody';
import { DialogFooter } from './DialogFooter';
import AlertWarningIcon from '@strapi/icons/AlertWarningIcon';
import DeleteIcon from '@strapi/icons/DeleteIcon';

<Meta title="Design System/Components/Dialog" component={Dialog} />

# Dialog

This is the doc of the `Dialog` component

[View source](https://github.com/strapi/parts/tree/develop/packages/strapi-parts/src/Dialog)

## Imports

```js
import { Dialog, DialogBody, DialogFooter } from '@strapi/parts/Dialog';
import { Row } from '@strapi/parts/Row';
import { Text } from '@strapi/parts/Text';
import { Stack } from '@strapi/parts/Stack';
import { Button } from '@strapi/parts/Button';
import DeleteIcon from '@strapi/icons/DeleteIcon';
import AlertWarningIcon from '@strapi/icons/AlertWarningIcon';
```

## Usage

Description...

<Canvas>
  <Story name="base">
    {() => {
      const [isVisible, setIsVisible] = useState(false);
      return (
        <>
          <Button onClick={() => setIsVisible(true)}>Click me</Button>
          <Box padding={10} background="neutral0">
            test overflow
          </Box>
          <Box padding={10} background="neutral0">
            test overflow
          </Box>
          <Box padding={10} background="neutral0">
            test overflow
          </Box>
          <Box padding={10} background="neutral0">
            test overflow
          </Box>
          <Box padding={10} background="neutral0">
            test overflow
          </Box>
          <Dialog onClose={() => setIsVisible(false)} title="Confirmation" isOpen={isVisible}>
            <DialogBody icon={<AlertWarningIcon />}>
              <Stack size={2}>
                <Row justifyContent="center">
                  <Text id="confirm-description">Are you sure you want to delete this?</Text>
                </Row>
              </Stack>
            </DialogBody>
            <DialogFooter
              startAction={
                <Button onClick={() => setIsVisible(false)} variant="tertiary">
                  Cancel
                </Button>
              }
              endAction={
                <Button variant="danger-light" startIcon={<DeleteIcon />}>
                  Confirm
                </Button>
              }
            />
          </Dialog>
        </>
      );
    }}
  </Story>
</Canvas>

## Props

<ArgsTable of={Dialog} />

### DialogBody props

<ArgsTable of={DialogBody} />

### DialogFooter props

<ArgsTable of={DialogFooter} />

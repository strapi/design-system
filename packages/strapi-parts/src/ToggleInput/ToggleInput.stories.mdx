<!--- ToggleInput.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { ToggleInput } from './ToggleInput';
import { Tooltip } from '../Tooltip';
import HelpIcon from '@strapi/icons/HelpIcon';

<Meta title="Design System/Components/ToggleInput" component={ToggleInput} />

# ToggleInput

This is the doc of the `ToggleInput` component

[View source](https://github.com/strapi/parts/tree/develop/packages/strapi-parts/src/ToggleInput)

## Imports

```js
import { ToggleInput } from '@strapi/parts/ToggleInput';
import { Tooltip } from '@strapi/parts/Tooltip';
import HelpIcon from '@strapi/icons/HelpIcon';
```

## Usage

Description...

<Canvas>
  <Story name="base">
    <ToggleInput
      hint="Enable provider"
      label="Enabled"
      name="enable-provider"
      labelAction={
        <Tooltip description="Content of the tooltip">
          <button
            aria-label="Information about the email"
            style={{ border: 'none', padding: 0, background: 'transparent' }}
          >
            <HelpIcon aria-hidden={true} />
          </button>
        </Tooltip>
      }
      onLabel="on"
      offLabel="off"
      checked={true}
      onChange={() => {}}
    />
  </Story>
  <Story name="error">
    <ToggleInput
      hint="Enable provider"
      error="this field is required"
      label="Label"
      name="enable-provider"
      onLabel="on"
      offLabel="off"
      checked={false}
      onChange={() => {}}
    />
  </Story>
</Canvas>

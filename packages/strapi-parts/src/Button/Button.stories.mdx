<!--- Button.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { ArgsTable, Description } from '@storybook/addon-docs';
import HelpIcon from '@strapi/icons/HelpIcon';
import ContentIcon from '@strapi/icons/ContentIcon';
import Ct from '@strapi/icons/Ct';
import { Button } from './Button';
import { Box } from '../Box';
import { Row } from '../Row';

<Meta title="Design System/Molecules/Button" component={Button} />

# Button

Buttons are a way to perform actions within a page. They help interact with its content via a single click. According to their style, specific labels and icons, they help understand the most important actions to take.

**Best practices**

- A button should have a relevant and clear label.
- Be positioned in consistent locations in the interface.
- If there are no primary button on a screen yet, this might be the one to look for.
- Prefer to have not more than two buttons side to side.

**When not to use**

- To navigate to another page in your browser (use Link component instead).

[View source](https://github.com/strapi/design-system-experiments/tree/develop/packages/strapi-parts/src/Button)

## Imports

```js
import { Button } from '@strapi/parts/Button';
```

## Usage

The most common button component used in the interface (primary action). It can have different variants and meaning.

<Canvas withSource="open">
  <Story name="default" parameters={{ docs: { source: { code: '<Button>Default button</Button>' } } }}>
    <Box paddingBottom={2}>
      <Button variant="default">Default</Button>
    </Box>
  </Story>
</Canvas>

### Button sizes

Main actions usually use a L button (top of a screen, footer of a modal, ...). For subsidiary action, a S button suits more (filters, configure the view, ...).

Use the size prop to change the size of the button. You can set the value to `S` or `L`.

<Canvas withSource="open">
  <Story
    name="sizes"
    parameters={{
      docs: {
        source: {
          code: `
<Button size="S">Small</Button>
<Button size="L">Large</Button>
`,
        },
      },
    }}
  >
    <Row>
      <Box paddingRight={1}>
        <Button size="S">Small</Button>
      </Box>
      <Button size="L">Large</Button>
    </Row>
  </Story>
</Canvas>

### Button variants

Use the variant prop to change the visual style of the Button.
You can set the value to `default`, `secondary`, `tertiary`, `success`, `danger`, `success-light`, or `danger-light`.

<Canvas withSource="open">
  <Story
    name="variants"
    parameters={{
      docs: {
        source: {
          code: `
<Button variant='default'>Default</Button>
<Button variant='secondary'>Secondary</Button>
<Button variant='tertiary'>Tertiary</Button>
...
  `,
        },
      },
    }}
  >
    <Row>
      {['default', 'secondary', 'tertiary', 'success', 'danger', 'success-light', 'danger-light'].map((variant) => (
        <Box key={variant} paddingRight={2}>
          <Button onClick={(e) => console.log(e)} variant={variant}>
            {variant}
          </Button>
        </Box>
      ))}
    </Row>
  </Story>
</Canvas>

#### Primary button

Use a Primary button to inform about the importance of an action. This is the strongest call to action. No more than one primary button should appear on a screen. 

Examples: Save, Finish, ...

#### Secondary button

Use a Secondary button mostly next to a primary button. This action has to be less important or is less likely to be perform by a user. 

Examples: Edit model, Configure the view, ...

#### Tertiary button

Use a Tertiary button when the action has a low impact or is the least likely to be perform by a user. 

Examples: Cancel, Close, ...

#### Success button

Use a Success button for reassurance. Use it preferably to indicate a validation, a success, an end of a workflow, ...

Examples: Confirm, Save, ...

#### Danger

Use a Danger button to indicate a destructive action. They are most commonly found in confirmation modals. More generally the red color and danger style indicate an important action that could not be reversible. 

Example: Delete

#### Success-light

Use a Success-light button to indicate a lower emphasis style to a successful action. 

#### Danger-light

Use a Danger-light button to indicate a lower emphasis style to a destructive action. 

Example: Delete this entry

### Button with icon

A button can be associated with an icon to give more impact.

You can add start and end icons to the Button component using the `startIcon` and `endIcon` props respectively.

<Canvas withSource="open">
  <Story
    name="icons"
    parameters={{
      docs: {
        source: {
          code: `
<Button startIcon={<HelpIcon />}>Information</Button>
<Button variant='secondary' endIcon={<ContentIcon />}>Write content</Button>
`,
        },
      },
    }}
  >
    <Row>
      <Box paddingRight={1}>
        <Button startIcon={<HelpIcon />}>Information</Button>
      </Box>
      <Button variant="secondary" endIcon={<ContentIcon />}>
        Write content
      </Button>
    </Row>
  </Story>
</Canvas>

### Button disabled

All action that cannot be performed yet.

<Canvas withSource="open">
  <Story
    name="disabled"
    parameters={{
      docs: {
        source: {
          code: `
<Button disabled startIcon={<HelpIcon />}>Information</Button>
`,
        },
      },
    }}
  >
    <Box paddingRight={1}>
      <Button disabled startIcon={<HelpIcon />}>Information</Button>
    </Box>
  </Story>
</Canvas>

## Accessibility

- Button has role of button.
- When Button has focus, `Space` or `Enter` activates it.

## Props

<ArgsTable of={Button} />

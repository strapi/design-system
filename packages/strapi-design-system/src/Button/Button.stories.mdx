<!--- Button.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import HelpIcon from '@strapi/icons/HelpIcon';
import ContentIcon from '@strapi/icons/ContentIcon';
import Ct from '@strapi/icons/Ct';
import { Button } from './Button';
import { Box } from '../Box';

<Meta title="Design System/Molecules/Button" component={Button} />

# Button

This is the doc of the `button` component

## Variants

Description...

<Canvas>
  <Story name="variants">
    {['default', 'secondary', 'tertiary', 'success', 'danger', 'success-light', 'danger-light'].map((variant) => (
      <Box key={variant} paddingBottom={2}>
        <Button onClick={(e) => console.log(e)} variant={variant}>
          {variant}
        </Button>
      </Box>
    ))}
  </Story>
</Canvas>

## Disabled

Description...

<Canvas>
  <Story name="disabled">
    <Box paddingBottom={2}>
      <Button disabled>Disabled</Button>
    </Box>
    <Button onClick={(e) => console.log(e)} startIcon={<ContentIcon />} disabled>
      Disabled
    </Button>
  </Story>
</Canvas>

## With icon

Description...

<Canvas>
  <Story name="with-icon">
    <Box paddingBottom={2}>
      <Button startIcon={<ContentIcon />}>Content type</Button>
    </Box>
    <Button endIcon={<HelpIcon />}>Help</Button>
  </Story>
</Canvas>

## L size

Description...

<Canvas>
  <Story name="lsize">
    <Button size="L" startIcon={<ContentIcon />}>
      Content type
    </Button>
  </Story>
</Canvas>

<!--- ToggleCheckbox.stories.mdx --->

import { useState } from 'react';
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { ToggleCheckbox } from './ToggleCheckbox';

<Meta
  title="Design System/Molecules/ToggleCheckbox"
  component={ToggleCheckbox}
/>

# ToggleCheckbox

This is the doc of the `ToggleCheckbox` component

## ToggleCheckbox

Description...

<Canvas>
  <Story name="base">
    {() => {
      const [val, setValue] = useState(false);
      return (
        <ToggleCheckbox onLabel={'On'} offLabel={'Off'} checked={val} onChange={() => setValue((s) => !s)}>
          The field is required?
        </ToggleCheckbox>
      );
    }}
  </Story>
</Canvas>

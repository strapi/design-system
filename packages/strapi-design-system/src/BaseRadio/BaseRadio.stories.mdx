<!--- Radio.stories.mdx --->

import { useState } from 'react';
import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs/blocks';
import { BaseRadio } from './BaseRadio';
import { RadioGroup } from './RadioGroup';

<Meta title="Design System/Technical Components/BaseRadio" component={BaseRadio} />

## Imports

```js
import { BaseRadio } from '@strapi/design-system/BaseRadio';
```

# BaseRadio

The `BaseRadio` component is the basis of the `Radio` component.

## Usage

<Canvas>
  <Story name="base">
    {() => {
      const [selected, setSelected] = useState();
      return (
        <div>
          <h2 id="trophy-champions">Make a choice</h2>
          <RadioGroup
            labelledBy="trophy-champions"
            onChange={(e) => setSelected(e.target.value)}
            value={selected}
            name="prefered-meal"
          >
            <div>
              <label htmlFor="pizza">Pizza</label>
              <BaseRadio value="pizza" id="pizza" />
            </div>
            <div>
              <label htmlFor="bagel">Bagel</label>
              <BaseRadio value="bagel" id="bagel" />
            </div>
          </RadioGroup>
        </div>
      );
    }}
  </Story>
</Canvas>

## Disabled

The BaseButton can be disabled.

<Canvas>
  <Story name="disabled">
    {() => {
      const [selected, setSelected] = useState();
      return (
        <div>
          <h2 id="trophy-champions">Trophy champion</h2>
          <RadioGroup
            labelledBy="trophy-champions"
            onChange={(e) => setSelected(e.target.value)}
            value={selected}
            name="prefered-meal"
          >
            <div>
              <label htmlFor="first">Mario</label>
              <BaseRadio value="first" id="first" />
            </div>
            <div>
              <label htmlFor="second">Luigi</label>
              <BaseRadio value="second" id="second" disabled={true} />
            </div>
            <div>
              <label htmlFor="third">Wario</label>
              <BaseRadio value="third" id="third" />
            </div>
          </RadioGroup>
        </div>
      );
    }}
  </Story>
</Canvas>


## Props

<ArgsTable of={BaseRadio} />

<!--- Stack.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { Stack } from './Stack';
import { Box } from '../Box';

<Meta
  title="Stack"
  component={Stack}
  decorators={[withDesign]}
  parameters={{
    design: {
      type: 'figma',
      url: 'TODO: Fill the according figma file',
    },
  }}
/>

# Stack

This is the doc of the `Stack` component

## Stack

Description...

<Canvas>
  <Story name="base">
    <Stack size={4}>
      <Box background="primary100" padding={4}>
        First
      </Box>
      <Box background="primary100" padding={4}>
        Second
      </Box>
      <Box background="primary100" padding={4}>
        Third
      </Box>
    </Stack>
  </Story>
</Canvas>

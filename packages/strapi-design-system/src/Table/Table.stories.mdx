<!--- Table.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { Table } from './Table';
import { Th, Td } from './Cell';
import { Tr } from './Tr';
import { Thead } from './Thead';
import { Tbody } from './Tbody';
import { Box } from '../Box';

<Meta
  title="Table"
  component={Table}
  decorators={[withDesign]}
  parameters={{
    design: {
      type: 'figma',
      url: 'TODO: Fill the according figma file',
    },
  }}
/>

# Table

This is the doc of the `Table` component

## Table

Description...

<Canvas>
  <Story name="base">
    {() => {
      const ROW_COUNT = 30;
      const COL_COUNT = 5;
      const rows = Array(ROW_COUNT - 1)
        .fill(null)
        .map(() =>
          Array(COL_COUNT)
            .fill(null)
            .map((e, x) => x),
        );
      return (
        <Box shadow="filterShadow" padding={3} hasRadius>
          <Table colCount={COL_COUNT} rowCount={ROW_COUNT}>
            <Thead>
              <Tr>
                <Th>
                  <Box padding={2} background="neutral200">
                    One
                  </Box>
                </Th>
                <Th>
                  <Box padding={2} background="neutral200">
                    Two
                  </Box>
                </Th>
                <Th>
                  <Box padding={2} background="neutral200">
                    Three
                  </Box>
                </Th>
                <Th>
                  <Box padding={2} background="neutral200">
                    Four
                  </Box>
                </Th>
                <Th>
                  <Box padding={2} background="neutral200">
                    Five
                  </Box>
                </Th>
              </Tr>
            </Thead>
            <Tbody>
              {rows.map((row, rowIndex) => (
                <Tr key={`row-${rowIndex}`}>
                  {row.map((cell) => (
                    <Td key={`cell-${rowIndex}-${cell}`}>
                      <Box padding={2}>
                        {rowIndex}/{cell}
                      </Box>
                    </Td>
                  ))}
                </Tr>
              ))}
            </Tbody>
          </Table>
        </Box>
      );
    }}
  </Story>
  <Story name="simple">
    {() => {
      const ROW_COUNT = 30;
      const COL_COUNT = 5;
      const rows = Array(ROW_COUNT - 1)
        .fill(null)
        .map(() =>
          Array(COL_COUNT)
            .fill(null)
            .map((e, x) => x),
        );
      return (
        <Box shadow="filterShadow" padding={3} hasRadius>
          <Table colCount={COL_COUNT} rowCount={ROW_COUNT}>
            <Thead>
              <Tr>
                <Th>
                  <Box padding={2} background="neutral200">
                    One
                  </Box>
                </Th>
                <Th>
                  <Box padding={2} background="neutral200">
                    Two
                  </Box>
                </Th>
                <Th>
                  <Box padding={2} background="neutral200">
                    Three
                  </Box>
                </Th>
                <Th>
                  <Box padding={2} background="neutral200">
                    Four
                  </Box>
                </Th>
                <Th>
                  <Box padding={2} background="neutral200">
                    Five
                  </Box>
                </Th>
              </Tr>
            </Thead>
            <Tbody>
              <Tr>
                <Td>2/1</Td>
                <Td>2/2</Td>
                <Td>2/3</Td>
                <Td>2/4</Td>
                <Td>2/5</Td>
              </Tr>
              <Tr>
                <Td>3/1</Td>
                <Td>
                  <a href="#">Link to somewhere</a>
                </Td>
                <Td>3/3</Td>
                <Td>3/4</Td>
                <Td>3/5</Td>
              </Tr>
            </Tbody>
          </Table>
        </Box>
      );
    }}
  </Story>
</Canvas>

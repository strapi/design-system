<!--- Text.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { H1, H2, H3, Text, Typography, Subtitle, EllipsisText } from './';
import { Box } from '../Box';
import { Divider } from '../Divider';
import { TEXT_VARIANTS } from './constants';

<Meta title="Design System/Technical Components/Text" component={Typography} />

# Text

This is the doc of the different Text / Typography components.

[View source](https://github.com/strapi/design-system/tree/develop/packages/strapi-design-system/src/Text)

## Imports

```js
import { H1, H2, H3, Text, TextButton, Subtitle } from '@strapi/design-system/Text';
```

## Usage

The main way to display text content on the screen

<Canvas>
  <Story name="base">
    <Box>
      <Typography variant="beta">Variants</Typography>
      <Divider />
      {TEXT_VARIANTS.map((variant) => (
        <Box key={variant} paddingTop={2} paddingLeft={4}>
          <Typography variant={variant}>{variant}</Typography>
        </Box>
      ))}
    </Box>
  </Story>
</Canvas>

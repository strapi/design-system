<!--- Badge.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { Badge } from './Badge';
import { Stack } from '../Stack';

<Meta
  title="Badge"
  component={Badge}
  decorators={[withDesign]}
  parameters={{
    design: {
      type: 'figma',
      url: 'TODO: Fill the according figma file',
    },
  }}
/>

# Badge

This is the doc of the `Badge` component

## Badge

Description...

<Canvas>
  <Story name="base">
    <Stack size={2}>
      <div>
        <Badge>Doc</Badge>
      </div>
      <div>
        <Badge>Video</Badge>
      </div>
      <div>
        <Badge>Image</Badge>
      </div>
    </Stack>
  </Story>
</Canvas>

import { Meta, Canvas, ArgsTable } from '@storybook/blocks';
import { Toggle } from '@strapi/design-system';

import * as ToggleStories from './Toggle.stories';

<Meta of={ToggleStories} />

# Toggle

- [Overview](#overview)
- [Usage](#usage)
- [Props](#props)
- [Variants](#variants)

## Overview

Enable users to switch a single option between on and off states

[View source](https://github.com/strapi/design-system/tree/main/packages/strapi-design-system/src/ToggleInput)

## Usage

```js
import { Toggle } from '@strapi/design-system';
```

<Canvas of={ToggleStories.Base} />

## Props

<ArgsTable of={Toggle} />

## Variants

### Base

<Canvas of={ToggleStories.Base} />

### Size S

Toggle input with `small` size.

<Canvas of={ToggleStories.SizeS} />

### Using `null` values

Where desirable you can supply a `null` value to the component. This may be helpful in
circumstances such whether or not a restaurant is open – `true` / `false` / not yet specified (`null`).
It's additionally possible to "clear" the value by passing an `onClear` handler and a `clearLabel`.
This label _will not show_ if the value is `null`.

<Canvas of={ToggleStories.Nullish} />

### With field

Toggle input with field hint or error

<Canvas of={ToggleStories.WithField} />

<!--- {{name}}.stories.mdx --->

import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { {{name}} } from './{{name}}';

<Meta
  title="{{name}}"
  component={ {{name}} }
/>

# {{name}}

This is the doc of the `{{name}}` component

## {{name}}

Description...

<Canvas>
  <Story name="base">
    <{{name}} />
  </Story>
</Canvas>
